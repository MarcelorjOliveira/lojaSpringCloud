server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MarcelorjOliveira/lojaSpringCloud
          search-paths: config-server/fornecedor, config-server/loja